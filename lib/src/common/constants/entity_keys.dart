/// [Class] constanst for [Entity] base shared properties keys access.
final class EntityKeys {
  /// Property binding for [Id].
  static const String id = 'id';

  /// Property binding for [Timestamp].
  static const String timestamp = 'timestamp';

  /// Property binding for [Discriminator].
  static const String discriminator = 'discriminator';
}
