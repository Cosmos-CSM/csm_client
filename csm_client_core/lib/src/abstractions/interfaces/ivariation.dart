/// Represents a variation object from a interfaced category of objects, used on
/// data transactions for generic operations.
abstract interface class IVariation {
  /// Unique specific object identification.
  late String discriminator;
}
